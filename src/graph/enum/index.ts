// Mirrors graph/structs: per-package enum type registrations (registerEnumType calls extracted
// from node files) live here. Empty for now — no node package currently registers an enum type.
export {};
